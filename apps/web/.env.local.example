NEXT_PUBLIC_API_URL=http://localhost:3001/api
COOKIE_NAME=soaswag_at

# Payments. true = mock test-payment form (local testing). false = Stripe card form.
NEXT_PUBLIC_PAYMENTS_TEST_MODE=true
# Only needed when NEXT_PUBLIC_PAYMENTS_TEST_MODE=false (real Stripe, test keys = sandbox):
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# Legacy Square vars (provider switched to Stripe; kept for reference):
# NEXT_PUBLIC_SQUARE_APPLICATION_ID=
# NEXT_PUBLIC_SQUARE_LOCATION_ID=
# NEXT_PUBLIC_SQUARE_ENVIRONMENT=sandbox