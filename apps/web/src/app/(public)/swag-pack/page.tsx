import { PackStudio } from "@/app/components/studio/PackStudio";

export const metadata = {
  title: "Build a Pack · Swaggeroo",
  description: "Build a custom branded swag pack — pick products, set quantities, add your logo, and review.",
};

export default function SwagPackPage() {
  return <PackStudio />;
}
